CREATE TABLE popups (
id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
config INT NOT NULL, 
field CHAR(100) NOT NULL,
label CHAR(100) NOT NULL,
foreign key (config) references configs(id)
);

SELECT * FROM popups;

ALTER TABLE popups ALTER COLUMN field TYPE VARCHAR(100);
ALTER TABLE popups ALTER COLUMN label TYPE VARCHAR(100);

DELETE FROM popups;
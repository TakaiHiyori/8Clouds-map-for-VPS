CREATE TABLE colors (
id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
config INT NOT NULL, 
text CHAR(100) NOT NULL,
color CHAR(100) NOT NULL,
icon CHAR(100) NOT NULL,
iconColor CHAR(100) NOT NULL,
foreign key (config) references configs(id)
);

ALTER TABLE colors ALTER COLUMN icon TYPE CHAR(100);

SELECT * FROM colors;

ALTER TABLE colors ALTER COLUMN text TYPE VARCHAR(100);
ALTER TABLE colors ALTER COLUMN color TYPE VARCHAR(50);
ALTER TABLE colors ALTER COLUMN icon TYPE VARCHAR(50);
ALTER TABLE colors ALTER COLUMN iconColor TYPE VARCHAR(50);
ALTER TABLE colors RENAME COLUMN iconColor TO icon_color;

DELETE FROM colors;
